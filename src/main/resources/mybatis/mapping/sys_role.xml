<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mfq.admin.web.dao.SysRoleMapper">
	<sql id="selectsql">
		id, rolename, roledesc
	</sql>
	<sql id="insertsql">
		rolename, roledesc
	</sql>

	<insert id="insertRole" parameterType="SysRole"
		useGeneratedKeys="true" keyProperty="id">
		<selectKey resultType="long" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID() AS uid
		</selectKey>
		insert into sys_role(
		<include refid="insertsql" />
		)values (#{rolename}, #{roledesc})
	</insert>

	<select id="queryAll" resultType="SysRole">
		select
		<include refid="selectsql" />
		from sys_role
	</select>

	<select id="queryRoles" resultType="SysRole">
		select
		<include refid="selectsql" />
		from sys_role where id in 
		<foreach collection="list" open="(" separator="," close=")"
			item="id">
			#{id}
		</foreach>
	</select>
	
	<select id="queryRole" resultType="SysRole">
		select
		<include refid="selectsql" />
		from sys_role where id=#{id}
	</select>

	<select id="queryRoleByName" resultType="SysRole">
		select
		<include refid="selectsql" />
		from sys_role where rolename=#{rolename}
	</select>

</mapper>